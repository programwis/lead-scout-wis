/** จำนวน lead ที่เลือกได้ — จำกัดตัวเลือกไว้ไม่ให้ยิง limit สูงเกินจริง (ดู README เรื่องเวลา/ค่าใช้จ่าย) */
export const LEAD_COUNT_OPTIONS = [5, 10, 20, 50] as const;

export const DEFAULT_LEAD_COUNT = 5;
